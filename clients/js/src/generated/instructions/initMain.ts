/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitMainInstructionAccounts = {
  user: Signer;
  tokenMint: PublicKey | Pda;
  nftPool: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
  rent?: PublicKey | Pda;
};

// Data.
export type InitMainInstructionData = {
  discriminator: Array<number>;
  maxLimited: boolean;
  unlinkable: boolean;
};

export type InitMainInstructionDataArgs = {
  maxLimited: boolean;
  unlinkable: boolean;
};

export function getInitMainInstructionDataSerializer(): Serializer<
  InitMainInstructionDataArgs,
  InitMainInstructionData
> {
  return mapSerializer<
    InitMainInstructionDataArgs,
    any,
    InitMainInstructionData
  >(
    struct<InitMainInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['maxLimited', bool()],
        ['unlinkable', bool()],
      ],
      { description: 'InitMainInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [204, 192, 140, 8, 71, 243, 223, 39],
    })
  ) as Serializer<InitMainInstructionDataArgs, InitMainInstructionData>;
}

// Args.
export type InitMainInstructionArgs = InitMainInstructionDataArgs;

// Instruction.
export function initMain(
  context: Pick<Context, 'programs'>,
  input: InitMainInstructionAccounts & InitMainInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'e7lSProtocol',
    'MyProgram1111111111111111111111111111111111'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    user: { index: 0, isWritable: true, value: input.user ?? null },
    tokenMint: { index: 1, isWritable: false, value: input.tokenMint ?? null },
    nftPool: { index: 2, isWritable: true, value: input.nftPool ?? null },
    systemProgram: {
      index: 3,
      isWritable: false,
      value: input.systemProgram ?? null,
    },
    rent: { index: 4, isWritable: false, value: input.rent ?? null },
  };

  // Arguments.
  const resolvedArgs: InitMainInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  if (!resolvedAccounts.rent.value) {
    resolvedAccounts.rent.value = publicKey(
      'SysvarRent111111111111111111111111111111111'
    );
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitMainInstructionDataSerializer().serialize(
    resolvedArgs as InitMainInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
